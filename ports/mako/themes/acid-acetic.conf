# Acid Acetic for mako
#
# Generated by acidify from ports/mako/acid.conf.tera. Do not edit; edit the
# template, or the palette in crates/acid-palette/src/lib.rs, and re-run
# `make ports`.
#
# Colours only; timeouts, geometry and fonts stay in your own config. Pull it
# in with an absolute or ~/-prefixed path:
#
#     include=~/.config/mako/acid-acetic.conf
#
# mako applies the last matching value, so put the include above anything you
# want to override and below anything you want to keep. The criteria sections
# below are self-contained, so they do not leak into the including file.

background-color=#000000
text-color=#f2e9d0
border-color=#45b6ff
progress-color=over #1e1e1e

[urgency=low]
text-color=#cdc7b8
border-color=#555555

[urgency=normal]
text-color=#f2e9d0
border-color=#45b6ff

[urgency=high]
text-color=#f2e9d0
border-color=#ff4536

# The "N more" placeholder recedes; it is chrome, not a notification.
[hidden]
text-color=#6f6f6f
border-color=#3a3a3a
